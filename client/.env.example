# Client environment variables
# Copy to .env for local overrides. Only VITE_-prefixed vars reach the browser.

# Base URL for the API. Leave empty in development — the Vite dev server
# proxies /api requests to the Express server (see vite.config.ts).
VITE_API_URL=

# Set to "true" only in the public read-only demo deployment. Leave unset
# (or "false") everywhere else, including local dev — this disables write
# controls in the UI and shows the "sample data" banner. The backend's
# DEMO_MODE is what actually enforces read-only-ness; this just matches
# the UI to it. See docs/data-model.md and lib/demo.ts.
VITE_DEMO_MODE=
